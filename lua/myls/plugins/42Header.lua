return {
    {
        "42Paris/42header",
        event = "InsertEnter",
    } --,
		-- opts = function ()
			-- local header = require("42header")

			-- header.setup({
				-- name = {
					-- user42 = vim.g.user42,
					-- mail42 = vim.g.mail42,
			-- },
		-- })
		-- end,
}
